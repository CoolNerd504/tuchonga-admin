rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // OPEN RULES - FOR DEVELOPMENT ONLY
    // DO NOT USE IN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

